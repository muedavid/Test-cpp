
Erzeuge eine neue Directory direkt auf Github:

Lade diese herunter: git clone http.....

Kopiere alle files in diese Directory



Füge alles auf Github hinzu:

Git add .
Git commit -m "...."
git push

Branch

git branch // alle branch die Vorhanden sind
git branch test // erzeugt neuer branch mit dem Namen test

git checkout test // wechslet zum Branch checkout

Nach Änderungen in diesem Branch: 
git add .
git commit -m "..."
git push --set-upstream origin test
